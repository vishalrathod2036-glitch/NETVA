FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    nmap curl wget netcat-openbsd ssh python3 python3-pip \
    net-tools iputils-ping dnsutils whois \
    && rm -rf /var/lib/apt/lists/*

CMD ["tail", "-f", "/dev/null"]
